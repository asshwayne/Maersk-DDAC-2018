@model IEnumerable<MaerskWebApp.Models.Vessel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.VesselName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.VesselCapacity)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VesselName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VesselCapacity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VesselID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VesselID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VesselID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
